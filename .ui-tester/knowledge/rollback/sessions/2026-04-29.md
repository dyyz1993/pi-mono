# Session: ui-test-rollback-1777451920
Date: 2026-04-29
Module: rollback
Scenarios: 2 passed / 0 failed

## Tested

### Scenario 1: 回滚消息 (Rollback Message) — PASSED
- **Setup**: New session, sent "你好", assistant responded with 5 tool-call cards
- **Action**: Clicked "回滚消息" on latest assistant card (4th rollback button, @e46)
- **Before**: User "你" + 5 assistant cards (last one still streaming at 16:50)
- **After**: User "你" + 1 assistant card ("你好" response only)
- **Verification**:
  - Messages before rollback point preserved (user msg + first response remain)
  - Messages after rollback point cleanly removed (4 extra tool-call cards gone)
  - No orphaned entries or floating elements
  - Send button re-enabled
  - UI looks clean and usable

### Scenario 2: 回滚全部 (Rollback All) — PASSED
- **Setup**: Switched to existing session "fork: Bash Incremental Counter" with completed conversation (5+ message exchanges about TypeScript/browser.ts code analysis)
- **Action**: Clicked "回滚全部" on latest assistant card (@e58)
- **Before**: Full conversation visible with rich content (file overview, types.ts, browser.ts sections)
- **After**: Clean empty state showing "开始对话吧" placeholder
- **Verification**:
  - ALL user/assistant messages removed
  - No rollback buttons visible (no message cards exist)
  - DOM confirms: hasMessages=false, hasRollback=false, placeholder=true
  - Session ready for new messages

## Screenshots Captured
1. `/tmp/rollback-01-initial-state.png` — Initial page load (existing session)
2. `/tmp/rollback-02-new-session.png` — After creating new empty session
3. `/tmp/rollback-03-after-msg1.png` — After sending "你好" (response generating)
4. `/tmp/rollback-03b-msg1-complete.png` — Msg1 response complete
5. `/tmp/rollback-04-after-msg2.png` — After sending msg2 (processing)
6. `/tmp/rollback-07-fresh-session.png` — Fresh empty session after reconnect
7. `/tmp/rollback-08-msg1-done.png` — Fresh session msg1 done (5 cards)
8. `/tmp/rollback-09-before-rollback.png` — State before rollback test
9. `/tmp/rollback-10-after-rollback-msg.png` — **AFTER ROLLBACK MESSAGE** (clean!)
10. `/tmp/rollback-11-bash-session.png` — Bash session before rollback-all
11. `/tmp/rollback-12-before-rollback-all.png` — Before rollback all
12. `/tmp/rollback-13-after-rollback-all.png` — **AFTER ROLLBACK ALL** (empty!)

## Findings
- Rollback functionality works correctly for both single-message and full-session rollback
- MessageCard header buttons (Fork, 回滚消息, 回滚全部, 折叠) are consistently accessible via snapshot refs
- No orphaned entries after rollback — cleanup is thorough
- Agent responses can be very slow (30s+), especially with multiple tool calls
- Browser session stability issue: navigated to `about:blank` once during long wait; reconnection required
- Snapshot can report stale button states; use JS eval to verify actual disabled/enabled state

## Updated
- Created `rollback/selectors.yml` with stable selectors
- Created `rollback/patterns.yml` with reusable test flows and tips
