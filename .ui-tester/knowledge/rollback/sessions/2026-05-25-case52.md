# Session: rollback-52-1779686379
Date: 2026-05-25
Module: rollback
Scenario: Case 5.2 - Rollback Blocked During Streaming

## Result: PASS

## Test Setup
- Fresh browser session
- Authenticated with demo-test-token
- Created new chat session
- Sent 2 messages: "Say exactly: BEFORE1", "Say exactly: BEFORE2"
- Waited for both responses to complete (verified streaming inactive)
- Installed JS monitor (window.__rbMon) sampling every 150ms

## Test Execution
1. Sent long streaming message: "Count from 1 to 50, one number per line..."
2. Monitor captured button states every 150ms throughout streaming
3. During streaming: scrolled to top, hovered cards, checked rollback buttons
4. Attempted to click disabled rollback button
5. Waited for streaming to complete
6. Verified rollback buttons re-enabled

## Key Metrics
- Total monitor samples: 1082
- Streaming samples: 738
- Non-streaming samples: 344
- All streaming samples had ALL rollback buttons disabled: TRUE
- Any enabled during streaming: FALSE

## Findings
1. ALL rollback buttons disabled during streaming (disabled=true, cursor=not-allowed)
2. Clicking disabled button has no effect - no overlay, no message loss
3. After streaming completes, rollback buttons become enabled (disabled=false, cursor=pointer)
4. All 6 messages preserved (no data loss)

## Screenshots
- case-5.2-01-setup.png: 2 turns before streaming
- case-5.2-02-during-streaming.png: During active streaming
- case-5.2-03-after-click-during-stream.png: After clicking disabled rollback
- case-5.2-04-after-stream-done.png: After streaming completes

## Technical Notes
- Rollback buttons use HTML `disabled` attribute
- CSS class "cursor-not-allowed" added during streaming
- Virtual scrolling means only visible cards in DOM
- Rollback buttons only visible on hover (mouseenter event)
- Chat scroll container: document.querySelector('.h-full.overflow-y-auto.overflow-x-hidden')
