# Session: rollback-edge-1779534088
Date: 2026-05-23
Module: rollback (edge cases)
Scenarios: 1 passed / 1 partial / 1 partial

## Tested
- TC-E01: Empty session no rollback buttons: PASSED
- TC-E02: Rollback then send new message: PARTIAL (backend works, UI refresh bug)
- TC-E03: Rollback all page integrity: PARTIAL (backend works, UI refresh bug + residual memory search)

## Findings
- **BUG**: Rollback confirmation click succeeds, backend removes messages correctly, but UI does not refresh/re-render. Page reload shows correct state.
- **BUG**: After rollback all, "搜索记忆" overlay text persists in chat area (residual memory prefetch display)
- **BUG**: "开始对话吧" placeholder does not appear when memory prefetch overlay is shown
- Rollback button click via `agent-browser click @ref` works for @e87/e90 (first two cards) but NOT for @e98 (third card)
- `eval` `.click()` does NOT trigger React onClick handlers - must use Playwright click via ref
- Agent responses are very slow (30-120s+), especially for "test" messages
- Send button stays disabled after response completes because textarea is empty - need to type something to check
- cardCount=0 + rollbackButtonCount=0 is the reliable empty state indicator
- Memory prefetch ("记忆搜索") overlay can obscure "开始对话吧" placeholder

## Updated
- Confirmed rollback button structure: each card has title="回滚消息"
- Confirmed rollback-all = rollback on first user card removes everything
- Added UI refresh bug finding
