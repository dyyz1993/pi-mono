# Session: rollback-jsonl-1779534085
Date: 2026-05-23
Module: rollback (JSONL fallback mode)
Scenarios: 0 passed / 3 failed

## Tested
- TC-J01: JSONL Rollback Message: FAILED
  - Reloaded page (F5) to load from JSONL storage
  - Sidebar showed "空闲" (no active pi process)
  - Clicked "回滚消息" on latest assistant card -> confirmation dialog appeared -> clicked "确认回滚"
  - BUG: Confirmation dialog closed but NO messages were removed
  - No RPC call was made (checked network monitor)
  - No fetch errors, no console errors
- TC-J02: JSONL Rollback All: FAILED
  - Clicked "回滚消息" on first card (assistant1) -> confirm -> no effect
  - All messages remained, no empty state
- TC-J03: JSONL Console Error Check: PASSED (no errors) but rollback FAILED
  - Injected error interceptor
  - No fetch errors, no console errors during rollback
  - No "Client not found" / "getTree" / "navigateTree" / "RPC error" in DOM
  - But rollback still didn't work

## Findings
- CRITICAL BUG: Rollback in JSONL fallback mode does NOT work
  - The confirmation dialog appears and closes correctly
  - But no RPC call is made to the backend
  - No messages are removed from the UI
  - The issue is likely that the rollback handler checks for an active pi process/client
    and silently skips the operation when none exists
- RPC panel shows: navigateTree calls are cancelled with "No active session"
- The app correctly loads messages from JSONL files after page refresh
- Memory search ("记忆搜索") triggers automatically when switching sessions
- File tool cards show "pending" status after JSONL reload (stale state)
- "空会话" sidebar label briefly changed to "工作中" during rollback attempts (possible side effect)

## Selectors Verified
- button:has-text('回滚消息'): WORKS (appears in snapshot, clickable)
- button:has-text('确认回滚'): WORKS (appears after clicking rollback, clickable)
- Sidebar "空闲" label: VISIBLE
- JSONL reload: WORKS (messages loaded correctly after F5)

## Bug Details
- Root cause hypothesis: The rollback confirm handler likely calls a client RPC method
  (e.g., client.rollback()) but the client is null/undefined in JSONL mode since there's
  no active pi subprocess. The handler probably catches the error silently or has an
  early return when no client is available.
- RPC call monitoring showed ZERO network requests during rollback+confirm sequence
- No error toasts, no console errors - completely silent failure
