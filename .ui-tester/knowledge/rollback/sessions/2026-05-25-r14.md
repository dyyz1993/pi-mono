# Session: ui-test-rollback-1779692818
Date: 2026-05-25
Module: rollback
Scenarios: Multi-file rollback overlay test

## Summary
Tested multi-file rollback overlay display. Created 3 files (test-a.txt, test-b.txt, test-c.txt), then modified/deleted/created in 2nd turn.

## BUG-MULTI-1 CONFIRMED: Rollback overlay shows "本轮无文件变更" for multi-file operations
- The second user message involved: modify test-a.txt, delete test-b.txt, create test-d.txt
- Clicking "回滚消息+代码" on that message opens overlay with "回滚消息 + 代码" title
- Overlay shows "回滚将恢复以下文件到之前的状态：" but then "本轮无文件变更"
- No file entries (A/M/D) displayed at all
- This is consistent with previous BUG-MULTI-1 finding - files from multi-file turns not tracked

## PASSING
- First message rollback guard works (no overlay when clicking on first message)
- "回滚消息" (message only) rollback shows correct overlay: "回滚确认 - 回滚将移除当前轮次的消息，确定继续吗？"
- Cancel button dismisses overlay correctly
- Cancel trick decompacts virtual-scrolled cards (6 cards after cancel)
- Model is responding (openai-completions/glm-4.7)

## Findings
- The model "zhipuai-2/GLM-4.7 (二号)" initially didn't respond (stop disabled but no output)
- Switched model context to openai-completions/glm-4.7 started working
- Connection issues resolved after reconnecting
- User message cards get compacted (removed from DOM) after assistant responds
- Card compaction makes it hard to find the target user message for rollback
- Cancel trick helps decompact cards

## Updated
- BUG-MULTI-1 still present in round r14
