# Session: rollback-3262-fresh-1779631225
Date: 2026-05-24
Module: rollback
Scenarios: 1 pass / 1 fail / 2 skip

## Tested
- Case 3.2: Rollback to Turn A then chat (branch behavior): FAIL
  - First-turn rollback clears ENTIRE conversation (0 cards), expected 2
  - Branch behavior after rollback works correctly (Turn E creates clean conversation)
- Case 3.3: Consecutive rollback + chat: PASS
  - Turn F added (4 cards), rollback to first Turn E (0 cards), Turn G branch (2 cards)
  - Consecutive rollback+chat cycle works correctly
- Case 6.1: Timeline view Rollback Chat: SKIP (no timeline UI)
- Case 6.2: Timeline view Rollback Code: SKIP (no timeline UI)
- Case 6.3: Timeline vs MessageCard input restoration: SKIP (no timeline UI)

## Findings
- FIRST-TURN ROLLBACK BUG CONFIRMED AGAIN: Rolling back the very first message clears all cards (0 remain)
- Input box correctly restored with rolled-back message text after first-turn rollback
- Branch behavior works: sending new message after rollback creates clean conversation
- Conversation compaction still occurs (older user cards hidden but remain in DOM, restored on scroll to top)
- PENDING PERMISSIONS BLOCK ROLLBACK: When tool calls are waiting for approval ("始终允许"), ALL rollback buttons become disabled
- Must approve pending permissions before rollback operations
- "显示时间线" setting does NOT create a separate timeline view with different rollback UI
- No TimelineTurn components exist in the current app
- EVAL CLICK LIMITATION: React onClick handlers don't fire with eval .click() for rollback confirm button - use Playwright @ref

## Updated
- patterns.yml: Added tips about permission blocking and timeline view findings
- selectors.yml: No changes needed
