---
paths:
  - "**/*.test.ts"
  - "**/*.spec.ts"
  - "**/test/**/*.ts"
description: "测试质量守则"
severity: "high"
scope: "project"
whenToUse: "编写或修改测试文件时生效"
---

# 测试质量守则

## 规则
- 每个测试用例必须独立运行，不依赖其他用例执行顺序
- Mock 必须在 afterEach 中清理，避免状态泄漏
- 异步测试必须设置合理超时（建议 10s）
- 集成测试必须使用独立的测试数据库/临时目录
- 覆盖率低于新增代码 80% 的 PR 不应合并
